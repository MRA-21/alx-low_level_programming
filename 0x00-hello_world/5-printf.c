#include <stdio.h>
/**
 * main-entry point of the program
 *
 * Return: o if eno errors,return non zero if errors
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art\n");
	return (0);
}
